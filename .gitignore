# 訓練產物（PDF 在 pdf/ 底下，那個要進版控）
out/
*.pt

# 完整語料 107 MB，超過 GitHub 單檔 100 MB 上限，不進版控（取得方式見 README）
# 隨 repo 發布的是 corpus_zhtw_20k.jsonl（14 MB）與 corpus_sample.jsonl（1.4 MB）
data/corpus_zhtw.jsonl

# 追蹤檔會在每次跑訓練時重新產生。
# 但保留一份範例讓人不用跑就能看（見 traces/sample/）
traces/*.txt
!traces/sample/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# 編輯器
.vscode/
.idea/
*.swp
.DS_Store

# 簡報產生器的相依套件
tools/deck/node_modules/
